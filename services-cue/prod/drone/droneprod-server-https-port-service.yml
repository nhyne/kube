apiVersion: v1
kind: Service
metadata:
  annotations:
    external-dns.alpha.kubernetes.io/hostname: drone.nhyne.dev
  labels:
    app: drone-server
    component: drone
    env: prod
  name: droneprod-server-https-port
  namespace: drone
spec:
  ports:
  - port: 443
    targetPort: 443
  selector:
    app: drone-server
    component: drone
    env: prod
type: LoadBalancer
